name: PR SemVer Check

on:
  pull_request:
    branches: [main, new-deploy-stuff, new-deploy-stuff-and-epreprod]
    types: [opened, reopened, synchronize, labeled, unlabeled, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  validate:
    uses: JSL-Inc/new-golden-path-workflows/.github/workflows/pr-semver-check.yml@main
