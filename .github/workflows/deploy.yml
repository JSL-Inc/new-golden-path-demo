name: Branch Delivery Pipeline

on:
  push:
    branches:
      - "develop-*"
      - "feature-*"
      - "release-*"
      - "hotfix-*"
      - main

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  delivery:
    uses: JSL-Inc/new-golden-path-workflows/.github/workflows/deploy.yml@condense-testing-branch

