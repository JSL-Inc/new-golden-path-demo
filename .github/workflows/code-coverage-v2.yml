name: Code Coverage

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review]

permissions:
  contents: read

jobs:
  coverage:
    uses: JSL-Inc/new-golden-path-workflows/.github/workflows/code-coverage.yml@main
